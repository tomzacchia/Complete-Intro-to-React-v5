// Parcel has presets for react however we can overwrite default
// settings for custom plugins
{
  // preset-env looks at browserslist in package.json for compiliation
  "presets": ["@babel/preset-react", "@babel/preset-env"],
  "plugins": ["@babel/plugin-proposal-class-properties"]
}
